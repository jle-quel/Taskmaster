'use strict'

const allProcess = (processNamesOrGroupName) => {
  // console.log(1)
}

const oneProcess = (processNamesOrGroupName) => {
  // console.log(2)
  
}

module.exports = {
  allProcess,
  oneProcess
}